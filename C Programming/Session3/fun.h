#ifndef FUN_H // for text replacement it is used when multiple definitions is used
              // as if he does not see it, saw it once on text replacement
#define FUN_H

extern int var;

void fun(int x,int y);

#endif